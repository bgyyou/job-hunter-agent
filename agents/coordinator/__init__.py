"""agents.coordinator 子包

`CoordinatorAgent` 在 `orchestrator.py`，mixin 散到 `state.py` /
`match_analysis.py` / `chat.py` / `tools.py`。调用方直接
`from agents.coordinator.orchestrator import CoordinatorAgent`。
"""